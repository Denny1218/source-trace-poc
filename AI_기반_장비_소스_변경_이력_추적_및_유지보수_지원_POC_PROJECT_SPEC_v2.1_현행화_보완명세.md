# AI 기반 장비 소스 변경 이력 추적 및 유지보수 지원 POC
## PROJECT_SPEC v2.1 현행화 보완 명세

- 기준 파일: `AI_기반_장비_소스_변경_이력_추적_및_유지보수_지원_POC_PROJECT_SPEC_v2.md`
- 문서 성격: 기존 v2 명세에서 현재 구현과 충돌하는 항목을 대체하는 현행화 보완 명세
- 최종 갱신일: 2026-07-31
- 우선순위: 본 문서와 기존 v2가 충돌하면 본 문서를 우선 적용한다.
- 파일 운영 권장: 검증 완료 후 본 내용을 기존 v2에 병합하고, 파일명은 계속 `...PROJECT_SPEC_v2.md`로 유지한다.

---

## 1. 현재 핵심 사용자 인터페이스

### 1.1 주 사용 인터페이스

주 사용 인터페이스는 **Source Trace VS Code Extension**이다.

```text
VS Code에서 함수 또는 코드 선택
→ Source Trace: 장비 변경 이력 조회
→ 질문 입력
→ Backend 분석
→ Git 함수 lifecycle 및 변경내역서 근거 Markdown 표시
```

Continue 연동은 선택적 호환 기능이다. Extension 기능 사용에 Continue 설치나 `config.yaml` 설정을 필수로 요구하지 않는다.

### 1.2 Web UI 역할

Web UI는 다음 목적으로 사용한다.

```text
시스템 상태 확인
장비 및 Git 저장소 관리
Git 동기화 및 변경 이력 검증
변경내역서 검색
PPT Cache 및 분석 상태 확인
Evidence Link 검증
운영 및 장애 확인
```

---

## 2. Extension 서버 및 장비 설정

### 2.1 최초 설정

Extension에서 최초 분석을 실행했는데 서버 또는 장비 설정이 없으면 설정 흐름을 시작한다.

```text
서버 주소 입력
→ 서버 연결 확인
→ 서버에 등록된 장비 목록 조회
→ 장비명을 보고 선택
→ 설정 저장
→ 원래 분석 요청 계속
```

사용자는 `equipment_id` 숫자를 미리 알거나 직접 입력할 필요가 없어야 한다.

### 2.2 설정 저장 정책

```text
서버 주소: 사용자 또는 Workspace 설정
장비 선택: Workspace 설정 우선
Workspace가 없으면 사용자 설정 사용
```

장비를 임의로 기본 선택하지 않는다. `equipment_id=1`과 특정 서버 IP를 환경 독립적인 기본값으로 사용하지 않는다.

### 2.3 장비 검증

분석 실행 전에 다음을 검증한다.

```text
서버 URL 형식
서버 연결 가능 여부
장비 선택 여부
현재 서버에 선택 장비가 존재하는지
```

잘못된 장비를 다른 장비로 자동 fallback하지 않는다.

### 2.4 Continue 설정 지원

Extension은 Continue의 `config.yaml`을 자동 수정하지 않는다.

대신 현재 Extension이 알고 있는 다음 값을 이용하여 Continue 설정에 삽입할 문구를 생성할 수 있다.

```text
서버 URL
분석 API URL
장비명
equipment_id
useOllama 설정
```

제공 기능:

```text
Continue 설정 문구 보기
설정 문구 클립보드 복사
Continue 설정 파일 열기
```

실제 삽입과 저장은 사용자가 수행한다. 장비 또는 서버 설정 변경 시 새 설정 문구를 다시 생성하도록 안내한다.

---

## 3. 함수 Git lifecycle 분석 정책

함수 또는 Symbol 질문에서는 단일 Top Commit만 표시하지 않고 전체 변경 흐름을 구성한다.

### 3.1 기본 원칙

```text
함수 최초 추가
핵심 기능 변경
개발 및 보조 변경
후속 유지보수
연관 이력
```

- 최초 추가는 parent commit에서 기존 정의 존재 여부를 확인하여 판정한다.
- 함수 최초 추가는 lifecycle에서 한 건만 허용한다.
- Merge commit과 실질 변경이 없는 후보가 lifecycle을 왜곡하지 않도록 한다.
- 가능한 경우 commit별 exact Diff를 사용한다.
- DB Diff가 부족하면 live `git show` fallback을 사용할 수 있다.
- Diff를 확보하지 못한 항목은 직접 확인된 변경으로 표현하지 않는다.
- PPT가 없어도 Git lifecycle은 누락하지 않는다.

### 3.2 사용자 결과 분류

사용자 Markdown은 최소 다음 단계로 구성한다.

```text
1. 최초 개발 및 기능 확정
2. 개발 및 보조 변경
3. 후속 유지보수
4. 연관 이력(필요한 경우)
```

내부 enum이나 `확인 필요 후보` 같은 개발자 중심 용어를 사용자 제목으로 사용하지 않는다.

---

## 4. 변경내역서 연결 모델

변경내역서는 함수 전체에 한 문서를 일괄 부착하지 않고, lifecycle의 **기능 단계** 또는 개별 commit에 연결한다.

### 4.1 연결 유형

```text
Commit 직접 근거
기능 배포 근거
개발 과정 참고
후속 유지보수 참고
관련 참고자료
```

### 4.2 Commit 직접 근거 조건

다음 조건을 모두 충족할 때만 허용한다.

```text
exact commit Diff 확보
대상 함수 범위 내 변경 확인
Diff의 변경 행위와 문서 As-Is/To-Be 일치
함수 또는 소스 경로 일치
시간 또는 배포 구간이 합리적으로 일치
```

Diff를 확보하지 못했거나 commit message만 일치하면 직접 근거로 표시하지 않는다.

### 4.3 기능 단계와 문서

하나의 함수에 여러 공식 문서가 존재할 수 있다.

예:

```text
초기 기능 적용 문서
→ 최초 개발 및 기능 확정 단계

후속 로직 삭제 문서
→ 후속 유지보수 단계
```

후속 유지보수 문서를 초기 함수 추가 commit의 기능 배포 근거로 연결하지 않는다.

### 4.4 문서 검색어 정규화

한글 기능 토큰은 단어 순서에만 의존하지 않는다.

```text
청소년 후불
후불 청소년
청소년후불
후불청소년
```

공통 토큰 후보를 찾되, 변경 행위를 구분한다.

```text
적용/추가/도입
수정/보완
삭제/제거
테스트/로그 정리
```

같은 기능 영역이라도 적용 문서와 삭제 문서는 별도 기능 단계로 유지한다.

### 4.5 시간 및 행위 호환성

문서와 commit 연결 시 다음을 평가한다.

```text
commit 날짜
문서 작성일
배포 예정일
적용 버전
함수/소스 일치
변경 행위 일치
```

수개월 또는 수년 차이, 반대 행위, 다른 기능의 날짜 하드코딩 삭제 등은 직접 연결 점수를 낮추거나 금지한다.

---

## 5. PPT 소스 및 함수 파싱

PPT의 `소스/함수` 영역은 다음 필드로 분리한다.

```text
related_source_paths
related_symbols
```

### 5.1 소스 경로

- `.c`, `.h`, `.cpp` 등 파일 경로만 포함
- 경로 prefix 정규화
- 제어문자 제거
- 중복 제거

### 5.2 함수 및 Symbol

- 함수명, 구조체명, 상수명
- 경로 문자열 제외
- `()`는 표시 단계에서 일관되게 처리
- 제어문자와 불필요한 줄바꿈 제거
- `Lib()`, `Card()`, `src()`처럼 경로 토큰에서 잘못 생성된 함수명 제외

---

## 6. 결과 Markdown 계약

### 6.1 한눈에 보기

최소 다음 정보를 표시한다.

```text
주요 기능
최초 개발 commit
주요 개발 변경 수
개발 및 보조 변경 수
후속 유지보수 수
공식 적용 문서
후속 유지보수 문서
공식 변경내역서 수
연결 유형별 건수
분석 신뢰도
```

함수 전체 대표 기능명은 마지막 또는 최고 점수 문서 제목 하나로 결정하지 않는다.

우선순위:

```text
초기 기능 배포 문서
최초 추가 commit 메시지
초기 개발 구간에서 반복되는 기능명
```

후속 문서는 별도 유지보수 제목으로 표시한다.

### 6.2 문서 집계 일관성

다음 위치는 같은 문서 컬렉션을 사용한다.

```text
한눈에 보기 문서 수
각 lifecycle 항목
공식 적용 문서 섹션
후속 유지보수 문서 섹션
참조 근거
Extension Output 집계
```

본문에서 사용한 문서가 참조 근거에 빠지거나, 두 문서가 본문에 있으면서 공식 문서 수가 1건으로 나오는 불일치를 허용하지 않는다.

### 6.3 불확실성 문구

서로 모순되는 표현을 사용하지 않는다.

금지 예:

```text
Diff 확보가 제한됨
+
Commit 직접 근거
```

내용이 없는 `분석 범위 및 참고사항`, `특이 제한 사항 없음`은 출력하지 않는다.

---

## 7. Extension Output 정책

Output Channel은 사용자에게 분석 진행 상황을 이해시키기 위한 화면이다. 내부 디버그 페이로드를 기본 출력하지 않는다.

### 7.1 기본 Output

```text
[시간] Source Trace 분석 시작
서버: ...
장비: ...
요청: 함수 변경 이력 조회
함수: ...
파일: ...

[시간] 요청 준비 완료
[시간] 서버 요청 전송
[시간] 분석 결과 수신
[시간] Git 변경 이력: N건
[시간] 공식 변경내역서: N건
[시간] 분석 완료 (소요 시간)
```

### 7.2 기본 Output에서 제외

```text
source_mode
selection_symbol
immediate_selection_chars
selected_text_chars
query_sent
recent_fallback
selection line range
raw request body
internal enum
ISO 형식 내부 timestamp
```

이 정보는 애플리케이션 로그 또는 명시적인 진단 모드에서만 출력한다.

### 7.3 진단 모드

필요하면 별도 설정으로 제공한다.

```text
sourceTrace.diagnosticLogging = false
```

기본값은 false다. true일 때만 민감하지 않은 내부 진단 정보를 출력하며, 선택 코드 원문·전체 Diff·토큰·비밀번호는 출력하지 않는다.

---

## 8. README 및 사용자 문서

Extension README는 현재 프로젝트의 사용자 안내만 포함한다.

제거:

```text
STEP 번호
MVP
POC 데모 과정
과거 Continue 비교
구현 단계 역사
```

포함:

```text
기능
VSIX 설치
최초 서버 및 장비 설정
사용 방법
서버 변경
장비 변경
현재 설정 확인
Continue 설정 문구 생성
Output 확인
오류 해결
관리자 사전 준비
```

---

## 9. 테스트 기준

최소 회귀 테스트:

1. 초기 적용 문서와 후속 삭제 문서가 별도 단계에 배치된다.
2. 후속 문서가 초기 commit 전체에 복제되지 않는다.
3. 적용/추가 commit과 삭제 문서가 직접 연결되지 않는다.
4. Diff 미확보 항목은 Commit 직접 근거가 되지 않는다.
5. 본문에 사용된 모든 문서가 참조 근거와 집계에 포함된다.
6. 관련 소스와 관련 함수가 분리된다.
7. 경로 토큰이 함수로 출력되지 않는다.
8. Output 기본 모드에 내부 디버그 페이로드가 없다.
9. 진단 모드에서만 내부 처리 정보가 출력된다.
10. Extension 서버·장비 설정 후 Continue config 문구를 생성할 수 있다.
11. Continue config는 자동 수정하지 않는다.
12. 기존 Git lifecycle 및 일반 검색 회귀가 없다.

---

## 10. 산출물 원칙

기능 수정 후 다음을 갱신한다.

```text
산출물 폴더
서버PC deploy
운영PC VSIX
Extension 사용자 가이드
PROJECT_SPEC v2
테스트 결과 문서
```

완료 보고에는 다음을 포함한다.

```text
정확한 원인
변경 파일
설계 정책 변경
추가 테스트
Backend/Frontend/Extension 테스트 결과
VSIX 버전 및 패키징 결과
서버PC/운영PC 산출물 반영 여부
PROJECT_SPEC v2 반영 여부
```

STEP 10은 별도 승인 없이 진행하지 않는다.
