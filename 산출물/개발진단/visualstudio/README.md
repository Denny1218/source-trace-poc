# Visual Studio 진단용 패키지 (공식 운영 설치 아님)

이 폴더의 VSIX는 메뉴 누락 원인 조사에 사용한 **개발/진단 산출물**입니다.

- MenuProbe / DirectTools / ControlTest / ResourceVersion2 probe / group-parent probe
- `source-trace-visualstudio-0.1.0-VS2022-NOT-OFFICIAL.vsix` — VS2022는 현재 POC 공식 배포 대상이 아님

운영 PC에는 `산출물/운영PC/visualstudio/`의 **0.1.3 두 파일만** 설치합니다.
`/setup`, registry, CTO, MenuProbe는 일반 운영 절차가 아닙니다.
